<resources xmlns:tools="http://schemas.android.com/tools">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->

    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/light_gray</item>
        <item name="colorPrimaryDark">@color/darkblue</item>
        <item name="colorAccent">@color/darkblue</item>
        <item name="android:statusBarColor" tools:targetApi="lollipop">@color/verydarkblueborder</item>
        <item name="android:typeface">serif</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="AppCompatAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorPrimary">@color/darkblue</item>
        <item name="colorPrimaryDark">@color/verydarkblueborder</item>
        <item name="android:windowBackground">@color/grey_dark</item>

        <item name="colorAccent">@color/darkblue</item>
        <item name="android:textColorPrimary">@color/darkblue</item>
        <item name="android:textColor">@color/darkblue</item>
        <item name="android:textSize">20sp</item>
        <item name="android:background">@color/light_gray</item>
    </style>
</resources>
